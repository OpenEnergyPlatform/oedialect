[tool:pytest]
# we could also pass these directly to pytest as command line args
addopts = --tb native -v -r fxX --maxfail=5 -p no:warnings

[sqla_testing]
requirement_cls = oedialect.requirements:Requirements
# TODO: this section must exist, even though we dont use a profile_file:
profile_file =
